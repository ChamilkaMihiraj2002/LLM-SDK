# SDK Core Dependency
httpx>=0.27.0

# Consumer Application Dependencies (for your web server)
fastapi>=0.110.0
uvicorn>=0.28.0

# Optional but Highly Recommended for local development
python-dotenv>=1.0.1